# GA4の分野まとめ

## まず結論

GA4を復習するときは、まず `計測の入口` `Webとアプリの観測点` `生データの読み方` `セッションの解釈` `数値差異の読み方` の順で戻ると整理しやすいです。UIだけでなく、何が計測されて何が欠落するかと、BigQuery export をどう読むかまで押さえると崩れにくくなります。

## どこで使うか

- GA4の数値を解釈するとき
- BigQuery export の生データを読むとき
- direct や session_start の違和感を調べるとき

## この分野の全体像

- `タグ未設置ページからGA4へ一時的にpage_viewを送る`
  - 最小の疎通確認を見るトピック。
- `GA4とBigQueryのグラフ分析でユーザー導線を読む`
  - 通常集計から導線構造へ広げる見方を見るトピック。
- `GA4のdirectセッションをBigQueryで調査する`
  - `direct` をどう切り分けて見るかの入口トピック。
- `GA4のevent_paramsを生データへ直接クエリするときの考え方`
  - raw直クエリで `UNNEST` をどう使い分けるかを見るトピック。
- `GA4のsession_startと他イベントパラメータの見え方`
  - `session_start` と共通パラメータの見え方を切り分けるトピック。
- `GA4のbatch送信とBigQueryでのセッション内順序復元`
  - `event_timestamp` だけでは足りない理由を見るトピック。
- `GA4のセッション数がBigQuery・探索・Looker Studio・Data APIでズレる理由`
  - `生データ` `GA4レポート面` `表示の器` を分けて差異を読むトピック。
- `Universal LinksとAndroid App Linksの仕組みとアプリ起動計測`
  - OSによるWeb・アプリ振り分けと、リンク受信・目的画面表示の実測、Web到達差分による補完推計を見るトピック。

## よくある疑問

### Q. GA4の `direct` セッションは、全部が直打ち流入なの？

A. そうではありません。`direct` には直打ちやブックマークだけでなく、参照元欠損や引き継ぎ漏れも混ざります。

### Q. `event_timestamp` だけでイベント順は見てよい？

A. 足りないことがあります。`batch_ordering_id` と `batch_event_index` まで含めて見た方が安全です。

### Q. BigQueryと探索でセッション数がズレたら、まず何を見る？

A. まず `同じ接続元か` を見ます。その後に `HLL++` `modeling` `Google Signals` `反映遅延` を切り分けます。

### Q. Universal LinksやApp Linksの起動数はWebのpage_viewだけで分かる？

A. 分かりません。アプリへ振り分けられると通常はWebページが表示されないため、アプリ側で `deep_link_received` と目的画面表示を計測し、観測できない範囲だけ補完推計します。

## 詳細リンク

- [タグ未設置ページからGA4へ一時的にpage_viewを送る](./20_トピック/タグ未設置ページからGA4へ一時的にpage_viewを送る.md)
- [GA4とBigQueryのグラフ分析でユーザー導線を読む](./20_トピック/GA4とBigQueryのグラフ分析でユーザー導線を読む.md)
- [GA4のdirectセッションをBigQueryで調査する](./20_トピック/GA4のdirectセッションをBigQueryで調査する.md)
- [GA4のevent_paramsを生データへ直接クエリするときの考え方](./20_トピック/GA4のevent_paramsを生データへ直接クエリするときの考え方.md)
- [GA4のsession_startと他イベントパラメータの見え方](./20_トピック/GA4のsession_startと他イベントパラメータの見え方.md)
- [GA4のbatch送信とBigQueryでのセッション内順序復元](./20_トピック/GA4のbatch送信とBigQueryでのセッション内順序復元.md)
- [GA4のセッション数がBigQuery・探索・Looker Studio・Data APIでズレる理由](./20_トピック/GA4のセッション数がBigQuery・探索・Looker%20Studio・Data%20APIでズレる理由.md)
- [Universal LinksとAndroid App Linksの仕組みとアプリ起動計測](./20_トピック/Universal%20LinksとAndroid%20App%20Linksの仕組みとアプリ起動計測.md)

## 更新履歴

- 2026-07-10: `analytics-foundations` からGA4分野を独立
- 2026-07-28: Universal LinksとAndroid App Linksの起動計測を追加
